 <section class="card">
 <div class="card-header">
        <a href="/post/{{id}}">{{title}}</a>

         <p class="card-body">
        {{content}}
        </p>
       
        <p> Post by {{user.username}} on {{format_date created_at}}</p>
       
    </div>
 </div>


  </section>