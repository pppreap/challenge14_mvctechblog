 <section class="card">
 <div class="card-header">
        <a href="/post/{{id}}">{{title}}</a>
       
        <p> Post by {{user.username}} on {{format_date created_at}}</p>
       
    </div>
 </div>
 <p class="card-body">
{{content}}
 </p>

  </section>