 <div class="comments">
  {{#each this}}
   <div class="card">
    <h2 class="card-header"></h2>
   <section class="comment card-body">
       <div class="tag">
        {{user.username}} on {{format_date created_at}}
       </div>
       <div class ="text">
        {{comment_text}}
        </div>
   </section>
   </div>
 {{/each}}
  </div>