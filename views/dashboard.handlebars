

{{#if posts.length}}
<section>
    <h3> Dashboard</h3>
    {{#each posts as |post|}}
  
        {{> post-info post}}

    
<a href="/dashboard/edit/{{post.id}}"><button class="edit-btn btn">edit post</button></a>
</section>
{{/each}}



<a href="/dashboard/create"><button type="submit"  class="btn">+ New Post</button></a>


{{/if}}